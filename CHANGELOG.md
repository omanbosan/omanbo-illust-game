# おまんぼさんイラストゲーム 開発履歴

> **⚠️ このファイルは上書き禁止。追記のみ行うこと。**
> 新しい変更は「## [日付] タイトル」の形式で末尾に追記する。

---

## システム構成

| 要素 | 詳細 |
|------|------|
| フロントエンド | GitHub Pages (`https://omanbosan.github.io/omanbo-illust-game/`) |
| バックエンド | Google Apps Script Web App |
| DB | Google Sheets（`scores` シート・`drawings` シート・`votes` シート） |
| 画像保存 | Google Drive（フォルダID: `1gXwYsxpKBqZ6tMKdNyOPGVvtQTcV1QgN`） |
| GAS URL | `https://script.google.com/macros/s/AKfycby0q8MVbN-Dk_fio8XOAqZW4XnxJC4_RNw4__XQOt57RgQUfAEjqkirPBHCHlZ30D4JoA/exec` |
| GASプロジェクト | `https://script.google.com/home/projects/1TeBoLRDrBZe9QthYYyjTeBpfdyGjFVG2ESgImPcqogTEnu1eKW0iyAlk/edit` |
| スプレッドシート | ID: `1nkGoMoNKc4opnD-Z_Z41I_4re8Brz7lFyBdNXJfpyIw` |

---

## ファイル構成

```
game.html       - メインゲーム画面
admin.html      - 管理者承認画面
gas-backend.gs  - Google Apps Script バックエンド
index.html      - トップページ
```

---

## 採点ロジック（最新）

| モード | 計算式 | 満点ライン（IoU） |
|--------|--------|-----------------|
| 鬼モード | `round(20 × min(1, IoU × 3.8))` | IoU ≈ 26% |
| 通常モード | `round(20 × min(1, IoU × 5.46))` | IoU ≈ 18% |
| イージーモード | `round(20 × min(1, recall × 7.0))` | recall ≈ 14% |

- 5パーツ × 20点 + ボーナス40点 = 最高140点
- 鬼モードのみ採点図鑑への登録・参加番号が発行される

---

## 変更履歴

---

### [2025年頃〜] 初期機能（compaction前の記録）

- ゲーム基本機能（お絵描きチャレンジ・採点チャレンジ）
- イージー / 通常 / 鬼モード の3段階
- Google Drive 画像保存
- 月次ランキング・アーカイブ機能
- 日次まとめメール通知

---

### [セッション1] iPad対応・UI改善

**コミット:** `f7567b8` `fdfcc53`

- **iPad 描画線ズレ修正**: `fh-canvas` の座標計算を `play-canvas.getBoundingClientRect() + _gameScale` 方式に変更
- **iPad ボタン効かない修正**: `_fhCanvasInitialized` フラグでイベントリスナー重複登録を防止
- **iPad キャンバス拡大**: 画面幅600px以上の場合、最大700pxまでキャンバスを拡大表示

```javascript
function applyGameScale() {
  const isTablet = window.innerWidth >= 600;
  const maxAllowed = isTablet ? Math.min(700, window.innerHeight - 220) : 460;
  _gameScale = Math.min(maxAllowed, window.innerWidth - 24) / 460;
}
```

---

### [セッション2] 著作権・パロディ表示変更

**コミット:** `dc4cbb6` `d710922` `8c89ea7` `350414b`

- 「人気のキャラクターは商品化検討中」記載を削除
- パロディ系キャラへの注意書きを追加
- 表現変遷: `「アンパンマン・ドラえもん」` → `「テレビや本に登場するキャラクター」` → **`「アニメキャラクターなどに似せた絵」`**（最終）
- 図鑑に「管理者の思い出図鑑に保存」メッセージ追加
- お友達図鑑に著作権注意書き追加

---

### [セッション3] 管理画面 ログイン保持

**コミット:** `7c420d6`

- localStorage でログイン状態を**30日間**保持
- 「ログアウト」ボタン追加
- セッションキー: `omanbo_admin_session`

---

### [セッション4] 採点チャレンジ 登録フォーム制御

**コミット:** `c5f5af3` `24f959f`

- イージー / 通常モードは登録フォームを**非表示**（鬼モードのみ登録可能）
- 鬼モード登録: Instagram・フォロー確認・商業利用同意の**3つすべて必須**
- 同意しないと登録ボタンが押せない

---

### [セッション5] 鬼モード 輪郭チラ見せ機能

**コミット:** `a492e45` `544a45c`

- 鬼モード開始時、**1パーツ目のみ 0.5秒だけ**輪郭位置を表示
- 2パーツ目以降は表示なし（簡単になりすぎるため）

```javascript
if (index === 0) {
  await drawAnswerImageGuide(ctx);
  await new Promise(res => setTimeout(res, 500));
  ctx.clearRect(0, 0, canvas.width, canvas.height);
}
```

---

### [セッション6] 採点図鑑 ロード改善

**コミット:** `24c17d3`

- ページ読み込み2.5秒後にバックグラウンドで先読み（prefetch）
- スケルトンローディング表示（6枚のシマーカード）
- 20件ずつページネーション（「もっと見る」ボタン）

---

### [セッション7] 参加番号システム導入

**コミット:** `009a783` `babe48f` `a4ec09f`

- 投稿時に**ランダム5桁の参加番号（10000〜99999）**を自動生成
- 生成した画像の右下に番号をスタンプして保存
- GASの `scores` / `drawings` シートに記録
- Instagram は任意入力に変更（番号で当選管理）
- 不正防止: 番号はランダムのため推測困難

```javascript
function generateEntryId() {
  return String(Math.floor(10000 + Math.random() * 90000));
}
```

---

### [セッション8] 鬼モード 採点multiplier調整

**コミット:** `2cc3c59`

- 採点が厳しすぎるとの判断で調整
- `2.94` → **`3.8`**（満点ラインが IoU 34% → 26% に緩和）

```javascript
const multipliers = { hard: 3.8, normal: 5.46 };
```

---

### [セッション9] 管理画面 参加番号・スプシ情報表示

**コミット:** `da9daea` `6275b6f`

- GAS: `getPendingScores` / `getPending` が `entryId` を返すように修正
- GAS: `getZukan` / `getZukanScoring` の `.reverse()` を削除 → **古い順（No.1が最初）** に変更
- 管理画面カードに参加番号をオレンジの大きなバッジで表示
- スプシの全情報をテーブル形式で表示（名前・Instagram・日付・難易度など）
- **参加番号での検索ボックス**追加

---

### [2026-06-13] 投票数のGAS記録・管理画面投票ランキング・ボタンUI整理

**コミット:** `177a4b9`

**GAS変更（要デプロイ）:**
- `votes` シート追加（列: no, 村人名, 票数, 最終投票日）
- `castVote(data)` 関数追加: 投票時に votes シートに記録（upsert）
- `getVoteRanking()` 関数追加: 票数降順で返す
- `doPost`: `type === 'vote'` ルーティング追加
- `doGet`: `type === 'voteRanking'` ルーティング追加

**game.html変更:**
- `castZukanVote()`: 投票時に GAS へも fire-and-forget で送信

**admin.html変更:**
- 「🏆 投票」モードボタン追加
- 投票ランキングテーブル表示（バーチャート付き・票数降順）
- ボタンスタイルを**透明背景＋枠線**のシンプルなデザインに統一

---

### [2026-06-13] 管理画面 スプシ情報表示強化

**コミット:** `6275b6f`

- 参加番号を大きなオレンジバッジで表示（採点・お友達両カード）
- 全スプシ情報をテーブル形式で表示
  - 採点カード: 名前 / Instagram / 投稿日 / 難易度
  - お友達カード: ペンネーム / Instagram / IG表示設定 / 投稿日
- 参加番号での絞り込み検索ボックス追加

---

### [2026-06-13] 投票取り消し・再投票機能追加

**コミット:** `3a3601c`

**GAS変更（要デプロイ）:**
- `castVote()` に `delta` パラメータ追加（+1 or -1）
- `doPost`: `type === 'unvote'` で `delta: -1` を渡す

**game.html変更:**
- 投票済みボタンを **「❤️ 取り消す」** に変更
- タップすると投票取り消し → 残り票数が戻り別キャラに再投票可能
- GAS にも取り消し（-1票）を送信

---

## GAS デプロイ履歴メモ

| 日付 | 理由 |
|------|------|
| セッション序盤 | 初期機能一式 |
| 2026-06-13午前 | entryId保存・図鑑順序修正・getPending系修正 |
| 2026-06-13午後 | votes シート・castVote・getVoteRanking 追加 |
| 2026-06-13夕方 | unvote（取り消し）対応 |

---

## よく使うリンク

- [スプレッドシート](https://docs.google.com/spreadsheets/d/1nkGoMoNKc4opnD-Z_Z41I_4re8Brz7lFyBdNXJfpyIw/edit)
- [Google Drive フォルダ](https://drive.google.com/drive/folders/1gXwYsxpKBqZ6tMKdNyOPGVvtQTcV1QgN)
- [GAS プロジェクト](https://script.google.com/home/projects/1TeBoLRDrBZe9QthYYyjTeBpfdyGjFVG2ESgImPcqogTEnu1eKW0iyAlk/edit)
- [本番サイト](https://omanbosan.github.io/omanbo-illust-game/)
- [管理画面](https://omanbosan.github.io/omanbo-illust-game/admin.html)

---

### [2026-06-27] 採点基準説明を初期画面に追加

**コミット:** `9199e9c`

- 採点チャレンジ開始前の画面に「📐 採点基準」説明ボックスを追加
- 5パーツ × 20点 = 100点満点の構成を明示
- 点数帯ごとの評価ラベルを表示（🌟18〜20点 / 👍14〜17点 / 😅8〜13点 / 💦0〜7点）

---

### [2026-06-27] 採点システム全面刷新・フリースタイルお絵描き追加

**コミット:** `974695a` `10f2716`

**採点システム刷新:**
- ボーナス40点廃止 → **純粋に5パーツ × 20点 = 100点満点**
- 採点結果にパーツ別テーブル表示（得点・バーチャート・評価絵文字）
- 鬼モード multiplier: `4.0`（満点ラインIoU=25%、上位約5%が95点超）
- 通常モード multiplier: `9.0`

**採点バグ修正 (コミット: `1271eb7`):**
- `scoreFreehandOverlap` の `lineWidth` を `Math.max(20, cfg.h*0.28)` → **`8`** に修正
- ガバ描きでも高得点が出てしまう問題を解消

**お絵描きチャレンジ フリースタイル追加:**
- ⭕丸型 / 🔲フリースタイル の切り替えボタンを追加
- フリースタイル時は正方形キャンバス・枠線のみ（SVG輪郭非表示）
- 保存画像もshapeに応じて丸/四角で書き出し

---

### [2026-06-27] 景品テキスト変更・地獄モード削除

- 鬼モード景品: `レザー調キーホルダー` → `コースター`
- 地獄モード（新キャラ）を試験追加したが品質不足のため完全削除


---

### [2026-06-27] 採点multiplierを管理画面から設定可能に

**コミット:** `67c0806`

**GAS変更（要デプロイ）:**
- `config` シート追加（列: key / value）
- `getConfig()` 関数: `hardMult` / `normalMult` を返す（デフォルト 2.0 / 4.5）
- `setConfig(data)` 関数: config シートに upsert
- `doGet`: `type === 'config'` ルーティング追加
- `doPost`: `type === 'setConfig'` ルーティング追加

**game.html変更:**
- 起動時に GAS から `_scoringMultipliers` を非同期取得
- GAS 取得失敗時はデフォルト値（hard: 2.0, normal: 4.5）を使用
- `scoreFreehandOverlap` が `_scoringMultipliers` を参照するように変更

**admin.html変更:**
- 「⚙️ 採点設定」モードボタン追加
- 鬼モード / 通常モード の multiplier 入力欄
- 入力値から「満点ラインとなるIoU%」をリアルタイム表示
- 💾 保存 / 🎲 ランダム設定（鬼: 1.5〜3.5 / 通常: 3.0〜7.0）/ ↩ デフォルト リセット ボタン

---

### [2026-07-03] 採点チャレンジ 右目・左目の表記改善

**コミット:** `a781e28`

- フリーハンド採点の描画順を「左目→右目」から「右目（向かって左）→左目（向かって右）」に変更
- 「右目・左目」だけだと画面上どちらか分かりにくいため、パーツラベルに「（向かって右）」「（向かって左）」を併記するよう全箇所で統一
  - 配置ステータス・採点結果テーブルなど幅が狭い箇所は `左目(右)` `右目(左)` の短縮表記

---

### [2026-07-03〜04] 採点チャレンジに地獄モードを追加（新キャラクター）

**コミット:** `cc2f545` `283382f` `062bb53`

以前 [2026-06-27] に試験追加して品質不足のため削除した「地獄モード」を、新キャラクター（メガネのひよこ）で再実装。鬼モードと完全に別の難易度として並立させる形。

**キャラクター素材について:**
- ユーザー提供の全身イラスト（1024×1024 PNG）を、AIによる再描画は一切せず、そのまま540×540にリサイズしてbase64埋め込み（`ANSWER_SVG_HELL`）。過去の失敗（顔の形が変わってしまう）を繰り返さないための方針。
- 6パーツ構成: 髪・左目（向かって右）・右目（向かって左）・鼻・メガネ・口 → 満点120点（鬼モードの5パーツ100点満点とは異なる）
- 各パーツの中心座標は元画像を目視で確認しながら手動で特定（`PARTS_HELL`）

**採点ロジック:**
- `scoreFreehandOverlap` は地獄モードでも鬼モードと全く同じ計算式（`_scoringMultipliers.hard` を使用）で採点されるよう分岐。Node.js上でシミュレートし、鬼/地獄で同一入力に対し同一スコアになることを検証済み
- 賞品判定・コメント判定のしきい値は「100点満点」を前提にしていたため、120点満点でも公平になるよう比率(%)ベースの判定に変更

**UI/演出:**
- 難易度選択ボタンに「😈 地獄モード」を追加。演出は鬼モードと同じ「最初のパーツのみ0.5秒チラ見せ→以降は真っ暗」を共有（地獄モードに専用の中間難易度は存在しない）
- 図鑑・ランキングは鬼モードと混ざらないよう、地獄モード専用のフィルタタブを追加（図鑑: 全部/鬼/地獄、ランキング: イージー/通常/鬼/地獄）
- 登録フォーム・参加番号発行・賞品表示は鬼モードと同様に有効化
- admin.html の難易度バッジ表示にも地獄モードを追加
- スマホでは全身画像のままだとパーツが小さく描きにくいため、チラ見せ・チラ見ボタンでは顔部分（髪〜口の340×340領域）だけを切り出してプレイキャンバスいっぱいに拡大表示。採点結果画面はズーム座標を全身座標に変換して全身表示に戻す
- フリーハンドは元々複数ストロークを重ねて描ける仕様だったが伝わりにくかったため、指示文に「線は何本引いてもOK」を明記（メガネ等の一筆で描き切れないパーツ対策）
- 画面上部のサブタイトルが地獄モードでも「おまんぼさん／100点満点」の固定表示だったバグを修正し、難易度に応じて動的に切り替わるように変更

**対象外・注意点:**
- 「🎯パーツ配置」モード（ドラッグでパーツを組み立てる別ゲームモード）は今回のスコープ外。地獄モードを選択していても配置モードは従来通りおまんぼさんキャラのまま
- GASバックエンドは変更不要（`difficulty` を汎用文字列として保存・フィルタする既存実装のため `'hell'` もそのまま通る）
- 動作確認は Playwright でヘッドレスブラウザを使い、地獄モード選択→カウントダウン→6パーツ描画→採点→結果画面表示までの一連の流れを実プレイして確認

---

### [2026-07-06] 地獄モード結果画面の丸輪郭バグ修正・採点レベル分離・採点前ニックネーム登録フローに変更

**GAS変更（要デプロイ）:**
- `getConfig()` / `setConfig(data)`: `hellMult` を追加（デフォルト 2.0）。これまで地獄モードは鬼モードの倍率(`hardMult`)をそのまま流用していたが、独立した設定項目として分離

**game.html変更:**
- `renderResultCanvas()`: 採点結果の「見本あり」合成画像（`result-composite`）で、地獄モードでも常に丸輪郭を描画してしまっていたバグを修正。前回コミット(`a23981a`)でプレイ中の背景と保存用「見本なし」画像は輪郭線化されていたが、結果表示用の合成キャンバスだけ丸輪郭の描画コードが残っていたため、口を描き終えて採点すると丸の中にキャラクターが表示される状態だった。地獄モードは白背景のみに変更（キャラクター画像は既存の見本オーバーレイでそのまま表示される）
- `_scoringMultipliers` に `hell` を追加し、`scoreFreehandOverlap` のしきい値計算で地獄モードは鬼モードと別の倍率を参照するよう分岐（`fetchScoringConfig` で `cfg.hellMult` を取得）
- 採点フローを変更: 鬼モード・地獄モードは「✅ 採点する！」を押すと、採点結果を表示する前にニックネーム入力モーダル（`prescore-modal`）を表示。ニックネーム（任意・未入力は「名無し」）・Instagram（任意）・フォロー確認（任意）・商業利用同意（必須）を入力後、採点と同時に図鑑登録申請（`saveScore()`）が自動実行されるように変更。従来は採点結果を見た後に別途フォーム入力＋「🏆 登録！」ボタン押下が必要だった
- 採点結果モーダルの登録欄（`score-register-section`）は入力フォームではなく「📮 図鑑登録を申請中...」→「✅ 登録完了！」のステータス表示のみに簡略化

**admin.html変更:**
- 「⚙️ 採点レベル設定」パネルに「😈 地獄モード」のプリセットボタン・倍率入力欄を追加（鬼モードと同じ0.5〜10の範囲・5段階プリセット）
- `buildPresetButtons()` を汎用化（モードを引数で受け取るように変更し、鬼/通常の2種類決め打ちだった分岐を解消）してhell分を追加しやすく整理
- `🎲 ランダム` ボタンも地獄モードの倍率を含めてランダム化するよう変更

**対象外・注意点:**
- 通常・イージーモードの採点フローや登録UIは変更なし（従来通り採点後に登録フォームは表示されない）
- 「採点運」バッジ（`updateScoringLuckUI`）は引き続き鬼モードの倍率のみを参照する仕様のまま据え置き（今回のスコープ外）

---

### [2026-07-25] 同意文言変更・図鑑の月次消失バグ修正・過去画像の復旧機能・プレイ統計を追加

**発端:** 本番のGASエンドポイントに直接アクセスして実データを確認したところ、`scores`シートに鬼モードのデータが0件、`zukanScoring`の承認済みデータも0件だった。原因は`archiveMonthlyRanking()`（毎月1日トリガー）が画像URL・難易度を保存しないまま`scores`シートを全削除していたバグと判明。

**game.html変更:**
- 採点前ニックネーム入力モーダルの同意文言を「この作品の商業利用に同意する（必須）」→「SNS公開に同意する（必須）」に変更（`consent-check`）
- `trackPlay(event, difficulty, score)` を追加（fire-and-forget、失敗は無視）。`setDifficulty()` から `select` イベント、`doScore()` の最後から `complete` イベントを送信し、鬼/地獄/通常/イージー全モード・登録の有無を問わずプレイ状況を計測できるように変更

**GAS変更（要デプロイ）:**
- **図鑑消失バグ修正**: `archiveMonthlyRanking()` が月次クリア前に承認済み行を新しい永久保存シート`scores_zukan_archive`（名前・スコア・日付・画像URL・難易度・採点レベル・参加番号）にコピーするよう修正。あわせて`scores_archive`にも難易度列を追加し、`getLastMonthRanking`の難易度フィルタが機能するように修正
- `getZukanScoring()`: `scores_zukan_archive`（過去分）+ `scores`（今月分・承認済み）を古い順に結合して返すよう変更。フロント（鬼/地獄フィルタタブ）は変更不要で両モードの過去分画像が表示されるようになった
- **過去画像の復旧機能**: `getUnlinkedScoreImages()`（Driveフォルダ内の`score_`画像のうち、どのシートにも紐付いていない孤立ファイルを一覧化）と`recoverScoreImage(data)`（管理者が名前・スコア・難易度を手動指定して`scores_zukan_archive`に追加登録）を追加。難易度は自動判定できないため必ず管理者が選択する運用
- **プレイ統計**: `logPlay(data)`（`plays`シートに日時・イベント種別・難易度・スコアを記録）と`getStats()`（難易度別の選択数・採点完了数、直近14日間の日別選択数、図鑑登録数、投票総数・投票された村人数を集計）を追加

**admin.html変更:**
- モード切替を2×2グリッドから3×2グリッドに変更し、「📊 統計」「🗂 復旧」を追加
- 統計パネル: 難易度別の選択数/採点完了数バー、直近14日間の日別プレイ数バー、図鑑登録数・投票総数のタイル表示（既存の投票ランキングで使っている`'█'.repeat(...)`バー表示の流儀を踏襲）
- 復旧パネル: 孤立画像をカード表示し、名前（ファイル名から自動抽出）・スコア・難易度（プルダウン、初期値なしで必須選択）を入力して「📖 図鑑に登録」できるようにする

**対象外・注意点:**
- 過去にすでに月次リセットで消えてしまった画像自体は自動復元できない（Driveに`score_`ファイルが残っていれば「🗂 復旧」から手動で図鑑に登録し直せる）
- お友達図鑑側の同意文言（図鑑登録フォーム内の「著作権・肖像権に同意する」）は今回変更していない

---

### [2026-07-26] お絵描きモードの月替わりお題・シェア画像へのURL/ハッシュタグ焼き込みを追加

**GAS変更（要デプロイ）:**
- `getConfig()` / `setConfig(data)`: `theme`（お絵描きモードの「今月のお題」自由入力テキスト、最大60文字）を追加。数値項目とは別に文字列としてconfigシートに保存

**game.html変更:**
- お絵描きチャレンジ画面に`#odekaki-theme`を追加し、管理画面で設定した`theme`を「🎨 今月のお題：〜」として表示（未設定時は非表示）。採点無し・フリー描画のまま、月替わりでお題文言だけ管理画面から差し替えられる運用
- `drawShareWatermark(ctx, w, h)` を追加し、採点チャレンジの結果画像（見本あり・見本なし両方）とお絵描きモードの保存画像に、下部に半透明バー＋`#おまんぼさんイラスト`とゲームURL（`omanbosan.github.io/omanbo-illust-game`）を焼き込むように変更。既存の投稿手順（③ `#おまんぼさんイラスト` でインスタに投稿）と同じハッシュタグを使用し、画像がSNS上で流通した際にゲームへの導線が残るようにした

**admin.html変更:**
- 「⚙️ 採点設定」タブの上部に「🎨 お絵描きモードの今月のお題」入力欄を追加。既存の「💾 保存」ボタンで採点倍率と同時に保存される

**対象外・注意点:**
- お題は月替わり運用を想定しているが、頻度に関する自動化（月初に自動でリセット等）は行っていない。管理画面から手動でいつでも書き換える運用
- 難易度選択・採点結果のフローには影響しない（お絵描きモードの表示のみ）

---

### [2026-07-26] お絵描きお題を「週替わり自動ローテーション」に変更（12ヶ月×4週・48種類）

**発端:** 手動での毎月書き換え運用ではなく、12ヶ月×週替わりで自動的に切り替わる仕組みにしたいという要望。おまんぼさん・ぽっぴー（地獄キャラ）が季節の行事をしている絵、というテーマで48種類のお題を用意。水着・入浴など肌の露出を連想させる表現（プール・海水浴・温泉）は使わず、こたつ・ひまわり畑・浜辺でのスイカ割りなど安全な表現に置き換えた。

**GAS変更（要デプロイ）:**
- 新しいシート`themes`（月・週・お題の3列）を追加。`setupThemesSheet()`（初回のみ実行・冪等）で12ヶ月×4週=48件を一括投入
- `getCurrentWeeklyTheme()`: 今日の日付（1〜7日=第1週・8〜14日=第2週・15〜21日=第3週・22日以降=第4週）から`themes`シートを参照して自動計算
- `getConfig()`: 返り値を`theme`（実際に表示する値）・`themeOverride`（手動上書きの生値）・`autoTheme`（自動計算値）の3種類に分離。手動上書きが空欄なら自動お題が使われる

**admin.html変更:**
- 「🎨 お絵描きモードのお題」欄に「今週の自動お題」を読み取り専用で表示するようにし、既存の入力欄は「特別な日用の手動上書き」として位置づけを明確化（`cfg.themeOverride`を表示するよう修正し、自動お題をそのまま保存してしまう不具合を回避）

**対象外・注意点:**
- 48件のお題文言は`themes`シートを直接編集すれば差し替え可能（管理画面UIからの一括編集機能は未実装）

---

### [2026-09-05] UI刷新（CSS変数化・デイリーストリーク・お題カード表示）＋「今週のお題」表記修正

**game.html変更:**
- `<style>`冒頭に`:root`を追加し、既存の配色（オレンジ/赤/黄橙/緑/紫）・角丸・シャドウ・トランジションをCSS変数化。`body`/`h1`/`#subtitle`/`.ctrl-btn`/`.modal-bg`/`.modal-box`など`<style>`ブロック内の既存ルールを変数参照に置き換え（本文中の大量のインラインstyleは対象外・据え置き）
- プレイ中画面の`h1`（採点/お絵描き画面共通ヘッダー）を、素朴な赤文字＋黄色影から、オープニング/モード選択画面と同系統のグラデーション文字に変更し、新旧デザインの断層を解消
- **デイリーストリーク機能を新規追加**（`updateDailyStreak()` / `renderStreakBadge()`、localStorage `omanbo_streak_v1`のみで完結・GAS変更不要）。`closeOpeningScreen()`実行時＝実際にプレイし始めたタイミングでカウントし、2日以上連続で`#streak-badge`（モード選択画面ヘッダー直下）に「🔥N日連続！」を表示
- **既知バグ修正**: お絵描きチャレンジの「今週のお題」表示が、バックエンドは週替わりロジックなのに文言が「今月のお題」のまま残っていた不具合を修正。変数名`_monthlyTheme`→`_weeklyTheme`にリネームし、`applyOdekakiTheme()`を単純なテキスト差し替えから`.theme-card`クラスによるカード型表示（ラベル＋お題テキスト）に強化
- オープニング画面の星パーティクル数を18→26個に増加。モード選択画面の採点/お絵描きチャレンジカードに`.mode-card-glow`クラス（ゆるやかな浮遊アニメーション、時間差付き）を追加

**admin.html変更:**
- 「🎨 お絵描きモードのお題」手動上書き欄のプレースホルダを、キャラクター名（おまんぼさん・ぽっぴー）を含む例文から、キャラクター非依存の例文（「好きな動物を描いてみよう」）に変更

**対象外・注意点:**
- 採点チャレンジ（鬼/通常/イージー/地獄モード）のキャラクター・座標採点ロジックは変更なし
- GAS側の変更は無いため、本エントリの変更は再デプロイ不要（GitHub Pagesへのpushのみで反映）

---

### [2026-09-05] お絵描きお題をキャラクター固定から自由ジャンルへ全面差し替え（要デプロイ）

**発端:** お絵描きチャレンジの週替わりお題48種類が「おまんぼさん・ぽっぴーが季節の行事をしている絵」というキャラクター固定になっていたのを、キャラクター縛りのない自由なお題に変更したいという要望。

**GAS変更（要デプロイ）:**
- `themes`シートに投入する48件のお題データを、キャラクター名を含まない自由ジャンル（🐾どうぶつ／🚗のりもの／🍙たべもの／🏞️しぜん・けしき／✨ファンタジー／🎒くらしの6ジャンル×8件、12ヶ月×4週へ均等ローテーション配置）に全面差し替え
- お題データを`getFreeThemesData()`関数に切り出し、`setupThemesSheet()`（初回セットアップ・冪等）はこれを参照するよう変更。データ投入処理は`writeThemesData(sheet, themesObj)`ヘルパーに共通化
- **`setupThemesSheet()`は既に`themes`シートが存在すると即returnする設計のため、本番の既存データはこの関数の再実行では差し替わらない。** 既存データを安全に総入れ替えする新関数`resetThemesToFreeThemes()`を追加（`themes`シートの2行目以降をクリアしてから再投入）。HTTP経由（`doGet`/`doPost`）には公開せず、GASエディタから手動で1回だけ実行する運用。実行前に`themes`シートをスプレッドシート上で手動複製してバックアップしておくこと
- `getCurrentWeeklyTheme()` / `getConfig()` / `setConfig()`は無変更（月・週キーで引く既存の仕組みをそのまま流用）

**game.html/admin.html変更:**
- 表記修正・プレースホルダ変更は上記「UI刷新」エントリに含む

**対象外・注意点:**
- 採点チャレンジ（鬼/通常/イージー/地獄モード）は対象外、キャラクター・座標データとも変更なし
- デプロイ手順: ①GASエディタにコードを反映 ②関数`resetThemesToFreeThemes`を手動で1回実行しログで48件投入を確認 ③「デプロイを管理」から新しいバージョンとしてデプロイ
